apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: __KubernetesNamespace__-shared-claim
  namespace: __KubernetesNamespace__
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: my-azurefile
  resources:
    requests:
      storage: __storage__